<h1>Albums</h1>
<div>
  <%= link_to 'New Album', new_album_path %>
  <hr />
  <% @albums.each do |album| %>
    <%= album.artist %>
    -
    <%= album.title %>
    <%= link_to 'Edit', edit_album_path(album) %>
    <br />
  <% end %>
</div>
